"""The demonstrations. One file each; every file here is found automatically.

To add one, copy `_template.py` to a new name and edit it. Files starting with
an underscore are skipped, which is how the template itself stays out of the
dashboard. Nothing else needs changing anywhere -- if you think it does, read
docs/ADDING_A_DEMO.md, and if it still does, that is a bug in demokit/.
"""
